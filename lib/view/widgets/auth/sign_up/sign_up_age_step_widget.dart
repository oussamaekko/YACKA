import 'package:flutter/material.dart';

class SignUpStepAge extends StatelessWidget {
  const SignUpStepAge({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text("Age")));
  }
}
